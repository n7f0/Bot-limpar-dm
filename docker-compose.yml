version: '3.8'
services:
  bot:
    build: .
    container_name: bot-limpar-dm-bot
    restart: always
    network_mode: bridge
    dns:
      - 8.8.8.8
      - 1.1.1.1
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    # NENHUM VOLUME MONTANDO /app