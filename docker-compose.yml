version: '3.8'
services:
  bot:
    build: .
    container_name: bot-limpar-dm-bot
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - data:/app

volumes:
  data:
